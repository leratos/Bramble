# Bramble runtime requirements.
#
# pyproject.toml is the packaging source of truth. This file exists for
# simple deployment/install flows that still expect requirements.txt.
fastmcp>=3,<4
starlette>=0.37,<2
jinja2>=3.1,<4
uvicorn>=0.30,<1
argon2-cffi>=23,<26
python-json-logger>=4,<5
